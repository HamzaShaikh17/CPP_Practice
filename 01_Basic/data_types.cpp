#include <iostream>

int main(){
    int a=2;
    std::cout<<"Value of a is "<<a<<" and size of a is "<<sizeof(a)<<std::endl;

    char ch='H';
    std::cout<<"Value of ch is "<<ch<<" and size of ch is "<<sizeof(ch)<<std::endl;

    bool b1=true;
    std::cout<<"Value of b1 is "<<b1<<" and size of b1 is "<<sizeof(b1)<<std::endl;

    double d=2.75;
    std::cout<<"Value of d is "<<d<<" and size of d is "<<sizeof(d)<<std::endl;


    return 0;
}